*.rds filter=lfs diff=lfs merge=lfs -text
*.pkl filter=lfs diff=lfs merge=lfs -text
*.csv filter=lfs diff=lfs merge=lfs -text

# Small hand-built analysis inputs (group mapping, ISO mapping, elasticities;
# under 7 KB each). Nothing in the model reproduces without them, so they are
# stored as ordinary git blobs rather than LFS pointers and survive a clone
# made without LFS. -text keeps bytes byte-exact. The bulk FAOSTAT inputs and
# all *.rds / *.pkl data keep their LFS routing unchanged.
FBS_Group_Mapping.csv -filter -diff -merge -text
faostat_country_mapping.csv -filter -diff -merge -text
elasticity_supply.csv -filter -diff -merge -text
elasticity_demand.csv -filter -diff -merge -text
child_energy_requirement_lookup.csv -filter -diff -merge -text

# Reference snapshots for the reproduction check. Small plain text, and the
# check is worthless if a clone cannot read them, so they are git blobs.
reference/*.csv -filter -diff -merge -text

# Survivor emissions with OECD factors, written by consumption_ghg.py. It is a
# required pipeline input that a clean clone cannot rebuild, because the UN WPP
# workbook it depends on is not redistributed here. Same reasoning as above:
# stored as a blob so it survives a clone made without LFS.
"mortality model total emissions_oecd.csv" -filter -diff -merge -text

# Survival weights for the food shock, written by survival_weighting.py. Same
# reasoning: compute_food_savings raises without it, so it must survive a clone
# made without LFS. Small (about 106 KB) and plain text.
data_result/food_shock_survival_weight.csv -filter -diff -merge -text

# Manuscript before/after-drug ratio table, written by scripts/build_ratio_table.py.
# Kept out of LFS for a different reason from the ones above: this table exists to
# be READ IN REVIEW. Its whole purpose is that a reader can see which ratios moved
# between runs, and in LFS `git diff` shows an opaque OID change instead of the
# numbers. At about 2 KB the pointer is a similar size to the content. Reversing
# this is one line, but the review cost is real -- comparing a committed
# data_result CSV against HEAD during this branch's work returned a 128-byte
# pointer and came close to being compared as if it were data.
data_result/ratio_table_before_after_drug.csv -filter -diff -merge -text

# Per-capita companion to the two waterfalls, written by
# scripts/build_per_capita_table.py. Same reasoning as the line above: a small
# results table whose point is to be diffed between runs.
data_result/per_capita_emissions_savings.csv -filter -diff -merge -text

# US share of year-1 food-emission savings, written by scripts/build_us_share.py
# and read by scripts/build_manuscript_numbers.R. Same reasoning again, with one
# addition: it is a collector INPUT, so if a clone without LFS hydrated it as a
# 130-byte pointer the R side would not fail -- read_csv would parse the pointer
# as a one-column table, the `nrow(r) != 1` guard would fire, and the workbook
# would come out silently missing its six US-share rows. At 317 bytes the pointer
# would also be larger than the content.
data_result/us_share_year1.csv -filter -diff -merge -text
